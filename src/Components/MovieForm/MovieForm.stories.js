import { MovieForm } from './MovieForm';
import '../../index.css';

export default {
  title: 'Movie/MovieForm',
  component: MovieForm,
  tags: ['autodocs'],
};

export const Default = {
  args: {

  }
};