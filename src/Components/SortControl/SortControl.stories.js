import { SortControl } from "./SortControl";
import '../../index.css';

export default {
  component: SortControl,
  tags: ['autodocs'],
};

export const Default = {};

export const Selected = {
  args: {
    selected: 'title',
  },
};